name: smartcatch_app
description: "Companion app for SmartCatch smartwatch (Android only)."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0-290.4.beta

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.35.0
  provider: ^6.1.0
  flutter_background_service: ^5.0.5
  flutter_local_notifications: ^18.0.1
  permission_handler: ^11.3.1
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
